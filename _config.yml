# GitHub Pages mirror — the zero-maintenance configuration, all of it in this file
# plus two small committed style files under _sass/. Theme: Just the Docs (pinned),
# which adds full-text search, a navigation sidebar, and documentation typography.
# Pages' own defaults do the rest: relative .md links become page links, the README
# serves as the front page, each page takes its title from its first heading, and
# every push republishes automatically. Nothing here needs editing when content changes.
title: "Model Act — Frontier AI Public Welfare Offenses"
description: "A public-domain model state statute extending responsible-officer liability to frontier AI — drafted in public, with its audits, errata, and full drafting record published beside it."

url: "https://frontieraiaccountabilityproject.github.io"
baseurl: "/model-act"
lang: en

remote_theme: just-the-docs/just-the-docs@v0.12.0
plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag    # canonical URLs, meta descriptions, Open Graph, JSON-LD — per page, automatic
  - jekyll-sitemap    # sitemap.xml for search engines, regenerated on every push

# Just the Docs
color_scheme: statute
search_enabled: true
heading_anchors: true
enable_copy_code_button: true
back_to_top: true
back_to_top_text: "Back to top"
aux_links:
  "The file repository — authoritative record":
    - "https://github.com/FrontierAIAccountabilityProject/model-act"
footer_content: "A publication of the Frontier AI Accountability Project · Public domain (CC0) · v3.4 tagged, checksums in the ledger · This site mirrors the repository; the repository is the authoritative record."

# The sidebar is a curated reading order, nine entries, not a directory listing:
# the reviewer page, the review packets, the long documents, the glossary, the map.
# Everything else stays one search or one link away. Each entry is named by exact
# path — the matcher takes whole paths, not prefixes.
#
# The packets are the one nested entry: "The review packets" carries the index page
# and every lane packet hangs beneath it as a child, so a fifth lane costs a click
# rather than a chapter. Children are matched to the parent by its exact title.
defaults:
  - scope:
      path: ""
    values:
      nav_exclude: true
  - scope:
      path: "README.md"
    values: { nav_exclude: false, nav_order: 0, title: "The front page — start here" }
  - scope:
      path: "REVIEWERS.md"
    values: { nav_exclude: false, nav_order: 1, title: "For reviewers" }
  - scope:
      path: "packets/index.md"
    values: { nav_exclude: false, nav_order: 2, title: "The review packets", has_children: true }
  - scope:
      path: "packets/criminal_law.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 1, title: "Criminal law" }
  - scope:
      path: "packets/enforcement.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 2, title: "Enforcement and prosecution" }
  - scope:
      path: "packets/security.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 3, title: "Frontier security" }
  - scope:
      path: "packets/fiscal.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 4, title: "Fiscal and administration" }
  - scope:
      path: "packets/federalism.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 5, title: "Federalism and preemption" }
  - scope:
      path: "packets/proportionality.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 6, title: "Proportionality and sentencing" }
  - scope:
      path: "packets/torts_design.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 7, title: "Torts and design" }
  - scope:
      path: "packets/open_source.md"
    values: { nav_exclude: false, parent: "The review packets", nav_order: 8, title: "Open source and academia" }
  - scope:
      path: "docs/the_case.md"
    values: { nav_exclude: false, nav_order: 3, title: "The case" }
  - scope:
      path: "docs/the_statute_translated.md"
    values: { nav_exclude: false, nav_order: 4, title: "The statute, translated" }
  - scope:
      path: "docs/questions.md"
    values: { nav_exclude: false, nav_order: 5, title: "Questions and answers" }
  - scope:
      path: "docs/the_definition.md"
    values: { nav_exclude: false, nav_order: 6, title: "Two definitions of \"frontier\"" }
  - scope:
      path: "docs/known_objections.md"
    values: { nav_exclude: false, nav_order: 7, title: "Known objections" }
  - scope:
      path: "docs/what_a_case_looks_like.md"
    values: { nav_exclude: false, nav_order: 8, title: "What a case would look like" }
  - scope:
      path: "standards/what_these_words_mean.md"
    values: { nav_exclude: false, nav_order: 9, title: "The glossary — what these words mean" }
  - scope:
      path: "MAP.md"
    values: { nav_exclude: false, nav_order: 10, title: "Everything — the full index" }
  - scope:
      path: "archive"         # superseded versions: reachable by link, never by search
    values:
      search_exclude: true
  - scope:
      path: "docs/01-the-problem.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/02-the-precedents.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/03-whats-in-the-act.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/04-how-to-hand-over-a-bill.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/05-where-and-when.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/06-track-record.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/07-context-timeline.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/08-behind-the-scenes.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "docs/09-the-government-caught-one.md"     # retired signpost: the link lands, search should not
    values:
      search_exclude: true
  - scope:
      path: "audit/record.md" # the 364KB frozen record would drown the search index
    values:
      search_exclude: true
